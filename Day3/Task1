function greet(name) {
  console.log("Hello " + name);
}
greet("Nivya");

function addAll(...nums) {
  let total = 0;
  for (let n of nums) {
    total += n;
  }
  console.log("Total:", total);
}
addAll(5, 10, 15);
